---
"@hedwigjs/devtools": patch
---

System Events renders the wire-level request trace (`request.forwarded`,
`response.received` with latency, `request.timeout`, `response.sent`)
with `sent` / `received` badges and the correlation id.
