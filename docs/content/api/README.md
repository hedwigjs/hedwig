# API Reference

Hand-written and (later) generated API docs per package. Populated when
`packages/*` publish their first versions.

Planned layout:

- `broker.md` — `@hedwigjs/broker` public API.
- `registry.md` — `@hedwigjs/registry` runtime + CLI flags.
- `devtools.md` — `@hedwigjs/devtools` component props + events.
- `adapters/*.md` — first-party transport adapters.

Not written yet.
