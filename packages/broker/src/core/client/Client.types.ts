/**
 * The public `Client` contract is owned by `@hedwigjs/client` (the SDK) and
 * re-exported here so host code can import it from the runtime as before.
 */
export type { Client, ClientOptions } from '@hedwigjs/client';
